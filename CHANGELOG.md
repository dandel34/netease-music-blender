# 更新记录

## v1.2.0

**修：Cycles 渲染时音频爆音/卡顿（EEVEE 正常）**

根因是 Blender 内置 `aud` 的默认混音缓冲只有 **1024 帧（≈23ms）**：Cycles 把 CPU 占满时，
混音线程一旦错过这个很短的期限就会丢帧爆音；EEVEE 走 GPU 所以听不出来。现在默认改成抗卡顿配置，
并留了三个可调项：

- **音频缓冲默认 8192 帧（≈186ms）**，可调 1024 ~ 32768，给混音线程 8 倍余量
- **播放前把音频缓存进内存**（`Sound.cache()`，约 20MB/分钟；超过 15 分钟自动跳过）；
  解码放在后台线程做（实测 4 分钟的歌要 0.2~2 秒，放主线程会卡界面）
- **音频后端可选**：自动 / WASAPI / OpenAL / SDL / PulseAudio / ALSA；
  指定后端不可用时自动退回默认后端并写日志，不会因此播不了

「播放」页新增「音频输出」一栏 + **一键抗卡顿** 按钮（= OpenAL + 8192 帧 + 内存缓存）；
偏好设置「音频（渲染时卡顿就调这里）」有同样三项；自检报告会打印真实生效的后端/缓冲/声道。

**验证**：本机（16 核）用另一个进程跑 Cycles 压满 CPU 做对照，位置指标下各配置时钟都不掉队
（最大落后 < 0.11 秒），但 1024 帧的余量只有 23ms；另外验证了非法缓冲值兜底、不存在的后端回退、
子线程解码，以及新默认下播放/暂停/跳转无回归（真机集成 24 → 25 步）。

## v1.1.1

**歌词只显示当前一句（可配置行数）**
- 默认改为 **1 行**：浮层只显示正在唱的这一句 + 它的翻译，底板也随之变矮（实测 133.8 vs 3 行的 218.1）
- 偏好设置 / 面板里可选 **1 / 3 / 5 / 7 行**，当前句永远居中（`lyrics.counts_for` 负责换算，偶数与非法值有兜底）
- 面板「歌词」一栏遵循同一设置：1 行时不再做「下一句」预告

**纯音乐 / 无歌词**
- 不再显示「暂无歌词」占位，整块只显示 **歌名 - 歌手**（用大号字，是主角；此时不受“显示歌名与歌手”开关影响，否则浮层就空了）
- 什么都没播时显示一句很淡的「♪ 未在播放 ♪」

**其它**
- 视口过小时仍然保证不越界：优先丢掉当前句之前的历史行，再丢末尾的后续行
- 测试：离线单测 67 → 72 项（新增行数换算与 1/3 行窗口）；真机集成 23 → 24 步（新增「歌词行数与纯音乐显示」，断言绘制列表里到底有什么文字）

## v1.1.0

**动态歌词浮层（新）**
- 在 3D 视图上叠一层跟随播放滚动的歌词：当前句高亮放大、上下文渐隐、显示翻译与歌名、底部带播放进度条
- **可拖动**：面板「拖动位置」进入拖动模式，左键确认、右键/ESC 取消、方向键微调（Shift 加速）；也可直接拖位置滑块，或一键重置
- **可开关**：面板开关按钮 + 快捷键 **Ctrl+Alt+L**（可在偏好里关掉）
- 外观可控：字号、背景不透明度（0 = 只画文字）、是否显示翻译、是否显示歌名与歌手、自定义字体
- 中文歌词无需额外字体：实测 Blender 内置字体自带 CJK 回退（24px 下「中文歌词」= 96×24）
- 播放时自动加载并解析 LRC（支持一行多时间戳、`[offset:]`、翻译按时间戳合并），面板与浮层共用一份时间轴
- 视口过小时自动减少行数并缩小字号，保证浮层不越界；绘制异常只记日志并显示在面板上，不影响 Blender 渲染

**缓存上限（新）**
- 默认最多保留 10 首，超出按「最久没用」淘汰（播放过的文件会标记为最近使用，即 LRU）
- 正在播放的那一首永远不会被删除；未下完的 `.part` 不参与统计
- 偏好设置里可开关、改数量上限（1～500）、手动「立即清理到上限」

**其它**
- `api.lyric()` 改为返回 `{lrc, translated, merged}`，浮层需要原始 LRC 来建时间轴
- 新增 `lyrics.py`（LRC 解析，纯标准库）与 `overlay.py`（布局纯函数 + GPU/blf 绘制）
- 测试：离线单测 37 → 67 项；真机集成 17 → 23 步，新增**图标名校验**（自动比对源码里的图标与 Blender 图标表）、中文字体度量、浮层布局（含极小视口不越界）、缓存上限运行时行为
- 修复：`enforce_cache_limit(keep=...)` 传单个路径字符串时会被按字符遍历，导致“正在播放的文件”保护失效；`cache_size` 与 `cache_files` 口径不一致（现在都忽略 `.part`）

## v1.0.0

首个版本。

**面板**
- 3D 视图侧栏独立分类「网易云音乐」，四个选项卡：账号 / 播放 / 歌单 / 推荐
- 歌单与歌曲两个可滚动列表，每行自带 ▶ 播放与 ♥ 收藏按钮

**登录**
- 仅 Cookie 导入（浏览器复制 `MUSIC_U` 等 Cookie 即可，任何网络环境都可用）
- 登录后显示昵称、用户 ID、会员状态；可选是否把 Cookie 存进 Blender 偏好

**音乐**
- 「我的歌单」（含收藏歌单），「我喜欢的音乐」自动置顶 + 独立按钮 + 大歌单补齐
- 「加入我喜欢的音乐」：列表行 ♥ 一键切换，播放页按钮，登录后自动同步全量 ♥；
  接口不可用时自动退回歌单加歌通道
- 每日推荐（未登录也可用）、私人雷达（三级自动定位 + 可手动指定歌单 ID）

**播放**
- 用 Blender 自带的 `aud` 模块在进程内播放，不依赖外部播放器
- 播放/暂停/停止、上一首/下一首、进度条拖动、±10 秒、音量、单曲/列表循环、自动下一首
- 播放队列管理、歌词、音质选择与自动降级、音频缓存（缓存命中不再下载）

**工程**
- 纯标准库实现 weapi / eapi 加密（AES-128 + RSA），FIPS-197 / NIST SP 800-38A 官方向量自检
- 三套接口协议（eapi / 普通 `/api` / weapi）按成功率自动降级
- 面板内「运行自检」可一键定位是网络问题还是接口变化
